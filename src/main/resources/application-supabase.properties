# =========================================
# Supabase (PostgreSQL) Production Profile
# =========================================
# Activate with: SPRING_PROFILE=supabase

spring.datasource.url=jdbc:postgresql://${SUPABASE_HOST}:${SUPABASE_PORT:5432}/${SUPABASE_DATABASE}
spring.datasource.username=${SUPABASE_USERNAME}
spring.datasource.password=${SUPABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# =========================================
# JWT Configuration - Production (15 min)
# =========================================
app.jwt.expiration=900000

# =========================================
# H2 Console - Disabled in Production
# =========================================
spring.h2.console.enabled=false

# =========================================
# CORS - Specific Origins Only
# =========================================
app.cors.allowed-origins=https://tu-dominio.com,https://www.tu-dominio.com

# =========================================
# Actuator - Production
# =========================================
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# =========================================
# Logging - Production
# =========================================
logging.level.com.vetivet=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
